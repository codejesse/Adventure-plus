import { ChannelsWrapper, ChannelType } from "../styles/ChannelStyles";

const Channels = () => {
    return (
        <ChannelsWrapper>
            <ChannelType />
            <ChannelType />
            <ChannelType />
            <ChannelType />
            <ChannelType />
        </ChannelsWrapper>
    );
}

export default Channels;